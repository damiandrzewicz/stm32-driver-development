#include "core/stm32f446xx.h"

namespace Core
{
    namespace Reg
    {
        //Def::GPIO_t* GPIOA    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOA);
        // // GPIO
        // Def::GPIO_t* GPIOA    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOA);
        // Def::GPIO_t* GPIOB    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOB);
        // Def::GPIO_t* GPIOC    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOC);
        // Def::GPIO_t* GPIOD    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOD);
        // Def::GPIO_t* GPIOE    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOE);
        // Def::GPIO_t* GPIOF    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOF);
        // Def::GPIO_t* GPIOG    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOG);
        // Def::GPIO_t* GPIOH    = reinterpret_cast<Def::GPIO_t*>(Addr::Periph::GPIOH);

        // // RCC
        // Def::RCC_t* RCC      = reinterpret_cast<Def::RCC_t*>(Addr::Periph::RCC);
        
    }
   
}